using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class _SetUpPlayer : MonoBehaviour
{
    public static _SetUpPlayer Player;

    // Use this for initialization
    void Awake()
    {
        Player = this;
    }

    // Update is called once per frame
    void Update()
    {

    }
}
